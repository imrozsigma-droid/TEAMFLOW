# teamflow

> Generated by [Backend Factory](https://backendfactory.dev) — production-ready backend in minutes, not months.

## What's Included

| Feature | Details |
|---------|--------|
| Entities | 6 (attachment, comment, invite, project, subscription, task) |
| Fields | 62 total |
| State Machines | 6 with 11 transitions |
| Auth | JWT + Row Level Security |
| Multi-tenant | Yes (tenant_id isolation) |
| Docs | API reference, guides, diagrams |

## ⚡ Quickstart (5 minutes)

### Prerequisites

- Node.js 20+
- A [Supabase](https://supabase.com) project (free tier works)

### 1. Install dependencies

```bash
cd teamflow
npm install
```

### 2. Configure Supabase

Copy `.env.example` to `.env` and fill in your Supabase credentials:

```bash
cp .env.example .env
```

Get your credentials from:
- **Supabase Dashboard** → Project Settings → **API** (for URL + keys)
- **Supabase Dashboard** → Project Settings → **Database** (for connection string)

```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
JWT_SECRET=your-jwt-secret
```

### 3. Run migrations

```bash
npm run migrate
```

This creates all tables, RLS policies, and indexes in your Supabase database.

### 4. Start the server

```bash
npm start
```

Your API is now running at `http://localhost:3000` 🎉

## 📋 Entities

### Attachment

| Field | Type | Required |
|-------|------|----------|
| task_id | uuid | ✅ |
| file_name | string | ❌ |
| file_key | string | ❌ |
| file_size | number | ❌ |
| content_type | text | ❌ |
| uploaded_by | string | ❌ |
| entity_type | text | ❌ |
| entity_id | uuid | ❌ |

**States:** active → archived

**Transitions:**
- `archived`: active → archived (roles: admin, member)

### Comment

| Field | Type | Required |
|-------|------|----------|
| task_id | uuid | ✅ |
| author_id | uuid | ✅ |
| body | text | ✅ |
| parent_id | uuid | ❌ |
| is_internal | boolean | ❌ |
| edited_at | timestamp | ❌ |

**States:** active → flagged → removed

**Transitions:**
- `flagged`: active → flagged (roles: admin, member)
- `removed`: flagged → removed (roles: admin, member)

### Invite

| Field | Type | Required |
|-------|------|----------|
| email | string | ✅ |
| role | string | ❌ |
| project_id | uuid | ✅ |
| token | string | ❌ |
| expires_at | datetime | ❌ |
| invited_by | string | ❌ |
| title | text | ❌ |
| description | text | ❌ |
| metadata | jsonb | ❌ |

**States:** pending → accepted → expired → revoked

**Transitions:**
- `accepted`: pending → accepted (roles: admin, member)
- `expire`: pending → expired (roles: admin, member)

### Project

| Field | Type | Required |
|-------|------|----------|
| name | string | ✅ |
| description | text | ❌ |
| deadline | string | ❌ |
| color | string | ❌ |
| start_date | text | ❌ |
| end_date | text | ❌ |
| budget | text | ❌ |
| progress | text | ❌ |
| is_archived | boolean | ❌ |
| metadata | jsonb | ❌ |

**States:** active → completed → archived

**Transitions:**
- `completed`: active → completed (roles: admin, member)

### Subscription

| Field | Type | Required |
|-------|------|----------|
| plan | string | ❌ |
| seats | number | ❌ |
| price_amount | decimal | ❌ |
| currency | string | ❌ |
| current_period_end | string | ❌ |
| workspace_id | uuid | ❌ |
| stripe_subscription_id | text | ❌ |
| stripe_customer_id | text | ❌ |
| stripe_price_id | text | ❌ |
| status | text | ❌ |
| current_period_start | text | ❌ |
| cancel_at_period_end | text | ❌ |
| canceled_at | timestamp | ❌ |
| trial_start | text | ❌ |
| trial_end | text | ❌ |
| metadata | jsonb | ❌ |
| invoice_number | text | ❌ |

**States:** trialing → active → past_due → cancelled

**Transitions:**
- `active`: trialing → active (roles: admin, member)
- `cancelled`: active → cancelled (roles: admin, member)

### Task

| Field | Type | Required |
|-------|------|----------|
| title | string | ✅ |
| description | text | ❌ |
| project_id | uuid | ✅ |
| assignee_id | uuid | ✅ |
| priority | string | ❌ |
| due_date | datetime | ❌ |
| estimate_hours | number | ❌ |
| assigned_to | text | ❌ |
| estimated_hours | text | ❌ |
| completed_at | timestamp | ❌ |
| tags | jsonb | ❌ |
| metadata | jsonb | ❌ |

**States:** todo → in_progress → in_review → done → blocked

**Transitions:**
- `in_progress`: todo → in_progress (roles: admin, member)
- `in_review`: in_progress → in_review (roles: admin, member)
- `done`: in_review → done (roles: admin, member)

## 🔌 API Endpoints

### Attachment

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/attachments` | List all |
| GET | `/api/attachments/:id` | Get by ID |
| POST | `/api/attachments` | Create |
| PUT | `/api/attachments/:id` | Update |
| DELETE | `/api/attachments/:id` | Delete |
| POST | `/api/attachments/:id/archived` | Transition: archived |

### Comment

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/comments` | List all |
| GET | `/api/comments/:id` | Get by ID |
| POST | `/api/comments` | Create |
| PUT | `/api/comments/:id` | Update |
| DELETE | `/api/comments/:id` | Delete |
| POST | `/api/comments/:id/flagged` | Transition: flagged |
| POST | `/api/comments/:id/removed` | Transition: removed |

### Invite

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/invites` | List all |
| GET | `/api/invites/:id` | Get by ID |
| POST | `/api/invites` | Create |
| PUT | `/api/invites/:id` | Update |
| DELETE | `/api/invites/:id` | Delete |
| POST | `/api/invites/:id/accepted` | Transition: accepted |
| POST | `/api/invites/:id/expire` | Transition: expire |

### Project

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/projects` | List all |
| GET | `/api/projects/:id` | Get by ID |
| POST | `/api/projects` | Create |
| PUT | `/api/projects/:id` | Update |
| DELETE | `/api/projects/:id` | Delete |
| POST | `/api/projects/:id/completed` | Transition: completed |

### Subscription

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/subscriptions` | List all |
| GET | `/api/subscriptions/:id` | Get by ID |
| POST | `/api/subscriptions` | Create |
| PUT | `/api/subscriptions/:id` | Update |
| DELETE | `/api/subscriptions/:id` | Delete |
| POST | `/api/subscriptions/:id/active` | Transition: active |
| POST | `/api/subscriptions/:id/cancelled` | Transition: cancelled |

### Task

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/tasks` | List all |
| GET | `/api/tasks/:id` | Get by ID |
| POST | `/api/tasks` | Create |
| PUT | `/api/tasks/:id` | Update |
| DELETE | `/api/tasks/:id` | Delete |
| POST | `/api/tasks/:id/in_progress` | Transition: in_progress |
| POST | `/api/tasks/:id/in_review` | Transition: in_review |
| POST | `/api/tasks/:id/done` | Transition: done |

## 🚀 Deploy

### Railway

```bash
npx @railway/cli login
npx @railway/cli init
npx @railway/cli up
```

### Docker

```bash
docker compose up -d
```

### Vercel (Serverless)

Add a `vercel.json` and deploy with `npx vercel`.

## 📖 Full Documentation

See the `docs/` folder for comprehensive guides.

---

*Generated by Backend Factory*
